import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alertCheckboxStyles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
